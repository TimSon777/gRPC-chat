syntax = "proto3";

package auth;

service Auth {
  rpc JwtProvider (LoginRequest) returns (LoginResponse);
}

message LoginRequest {
  string userName = 1;
}

message LoginResponse {
  string accessToken = 1;
}
